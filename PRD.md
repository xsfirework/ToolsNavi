# Ref Hub – 项目需求文档（PRD）

## 0. 产品一句话定义

这是一个**公开可访问、对 SEO 友好、无需登录**的链接/资源聚合站点，  
由站长单人维护，**首页偏展示、分类页偏效率、详情页极简**。

---

## 1. 背景与目标（SEO 友好）

本项目是一个**公开可访问的链接/资源聚合型网站**，用于集中展示和维护各类推荐链接、权益入口或实用资源。

网站**不要求访问者登录或注册**，任何人均可直接浏览、搜索并打开链接。  
在保持结构简单、维护成本低的前提下，网站**支持被搜索引擎正常收录**，以获取一定的自然搜索流量。

项目的主要目标是：
- 以清晰、直观、移动端可用的方式组织和展示链接资源
- 让访问者能够通过搜索引擎或站内搜索快速找到目标入口
- 不追求内容生产或社区互动，而是作为“高质量入口导航页”存在

---

## 2. 用户角色

### 2.1 站长（唯一维护者）
- 负责新增、编辑、分类和排序链接
- 不具备前端或复杂工程背景
- 希望维护过程简单、直观、可长期使用

### 2.2 访问者（公众）
- 无需登录即可访问全部内容
- 主要行为：浏览、搜索、点击跳转
- 使用桌面端为主，移动端可正常使用

---

## 3. 使用场景

- 访问者通过首页浏览推荐链接
- 访问者通过分类页快速查找目标资源
- 访问者通过搜索引擎搜索关键词进入分类页或链接详情页
- 站长新增或更新链接后，对外分享统一入口页面

---

## 4. 功能范围与版本规划

### 4.1 MVP（V1）功能范围

#### ✅ 包含功能
- 首页（卡片型，用于展示与发现）
- 分类页（左侧分类栏 + 右侧列表）
- 链接详情页（独立 URL，极简结构）
- 站内搜索（基于链接名称和描述）
- 管理后台（`/admin` + 简单口令）
- 链接管理（新增 / 编辑 / 删除 / 排序）
- 分类管理（新增 / 编辑 / 排序）
- SEO 基础支持（title / meta description / 可收录 URL）

#### ❌ 明确不包含（MVP 不做）
- 访问者登录 / 注册
- 权限系统 / 私密链接
- 点击统计 / 数据分析
- 数据库 / 多管理员 / 协作

---

## 5. 页面结构（MVP）

### 5.1 公共页面
- `/`  
  首页，展示推荐链接卡片，支持搜索

- `/category/:slug`  
  分类页，左侧分类栏 + 右侧链接列表（SEO 可收录）

- `/link/:slug`  
  链接详情页，展示极简说明并跳转外部链接

### 5.2 管理页面
- `/admin`  
  管理入口，通过环境变量中的口令访问

- `/admin/links`  
  链接管理页面（CRUD）

- `/admin/categories`  
  分类管理页面

---

## 6. UI 与页面布局

### 6.1 整体风格
- 偏工具型、克制、信息密度适中
- 首页用于展示，分类页用于效率
- 桌面端体验优先，移动端可正常使用

---

### 6.2 首页（混合型 · 卡片展示）
- 顶部：站点标题 + 搜索框
- 内容区：链接卡片列表
- 卡片内容：
  - 图标
  - 标题
  - 1 行简要说明
  - “查看详情”按钮

---

### 6.3 分类页（工具型）
- 左侧：分类列表（高亮当前分类）
- 右侧：链接列表（列表型）
- 列表项内容：
  - 链接标题
  - 简要说明（可选）
  - 查看详情入口

---

### 6.4 链接详情页（极简）
- 标题
- 1–3 行说明文字
- 明显的外部跳转按钮（如“前往官网”）

不支持 Markdown 或复杂排版，以降低维护成本。

---

## 7. 技术与实现约束（必须遵守）

- 使用 Next.js（App Router）
- 使用 Tailwind CSS
- **不使用数据库**
- 所有数据从 `data/*.json` 文件读取
- 后台管理通过 `/admin` + `.env.local` 中的 `ADMIN_PASSWORD`
- 修改链接或分类只需要改数据文件，不改代码

---

## 8. 数据结构约定（JSON）

### categories.json
```json
[
  {
    "id": "tools",
    "name": "工具",
    "slug": "tools",
    "order": 1
  }
]
